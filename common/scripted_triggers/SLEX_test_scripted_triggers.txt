# Scripted triggers using variables are inverted for some reason
# So we invert them per hand

SLEX_test_trigger = {
    NOT = { # This goes away with the inversion
        check_variable = {
            which = $LIST$_$INDEX$
            value > $VALUE$
        }
    }
}
